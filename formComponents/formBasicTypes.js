const file = {
  type: 'string',
  format: 'data-url'
}

const input = {
  type: 'string'
}

const dropDownOrRadio = {
  type: 'string',
  enum: [],
  enumNames: []
}

module.exports = {
  file,
  input,
  dropDownOrRadio
}
